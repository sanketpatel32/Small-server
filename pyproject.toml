[project]
name = "small-server"
version = "0.1.0"
description = "A tiny REST API testing server with SQLite, auth, and a built-in UI."
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
]

[tool.fastapi]
entrypoint = "main:app"
